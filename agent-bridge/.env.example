# Firebase Configuration
FIREBASE_PROJECT_ID=alchemist-e69bb
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json

# WhatsApp Business API Configuration
WHATSAPP_ACCESS_TOKEN=your-permanent-access-token
WHATSAPP_APP_ID=your-facebook-app-id
WHATSAPP_APP_SECRET=your-facebook-app-secret
WHATSAPP_API_VERSION=v18.0

# Webhook Configuration
WEBHOOK_VERIFY_TOKEN=your-webhook-verify-token
WEBHOOK_BASE_URL=https://your-domain.com

# Note: Agent webhook URLs are configured per account during creation
# No global agent URL needed - each deployment provides its own webhook URL
# Note: WhatsApp numbers are now provided per agent during account creation

# Collection Names (optional, will use defaults)
MANAGED_ACCOUNTS_COLLECTION=whatsapp_accounts
WEBHOOK_LOGS_COLLECTION=whatsapp_webhook_logs

# Development settings
DEBUG=false
LOG_LEVEL=INFO